{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions, scoped to the single main window only (see `windows` below): the app's IPC commands are reachable from that one trusted local webview and no other. The webview loads only the bundled frontend under a strict CSP (no remote or inline script), which is the boundary that keeps a would-be XSS from reaching these commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
